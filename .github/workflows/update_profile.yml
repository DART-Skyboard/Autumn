name: Update Profile SVG

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  update-profile:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Autumn
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate and commit profile.svg
        env:
          LEATR_ASH_PAT: ${{ secrets.LEATR_ASH_PAT }}
          GITHUB_TOKEN:  ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/generate_profile.js

      - name: Push profile.svg via git
        run: |
          git config user.name  "Autumn Profile Bot"
          git config user.email "autumn@dartmeadow.com"
          git add profile.svg
          git diff --staged --quiet || git commit -m "profile: update user count $(date -u +%Y-%m-%dT%H:%M)"
          git push
